@page
@model EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Testimonials</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Testimonial.Id" />
            <div class="form-group">
                <label asp-for="Testimonial.ServiceId" class="control-label"></label>
                <select asp-for="Testimonial.ServiceId" class="form-control" asp-items="ViewBag.ServiceId"></select>
                <span asp-validation-for="Testimonial.ServiceId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Testimonial.Name" class="control-label"></label>
                <input asp-for="Testimonial.Name" class="form-control" />
                <span asp-validation-for="Testimonial.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Testimonial.Content" class="control-label"></label>
                <input asp-for="Testimonial.Content" class="form-control" />
                <span asp-validation-for="Testimonial.Content" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Testimonial.DateCreated" class="control-label"></label>
                <input asp-for="Testimonial.DateCreated" class="form-control" />
                <span asp-validation-for="Testimonial.DateCreated" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
